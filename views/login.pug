extends layout

block content
  p
    form(method='POST' action='/searchSubmit/')
      div.grid-x.grid-padding-x
        div.cell.small-3
        div.cell.small-6(style='width: 50%')
          h1.display-4.m-b-2= title
  
          // sign in form
          div.input-group
            a.button.alert(href='/auth/google') Google
          
          div.input-group
            a.button(href='/auth/facebook') Facebook